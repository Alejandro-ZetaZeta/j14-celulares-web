[auth]
allowed_redirect_urls = ["http://localhost:3000/api/auth/callback", "https://celulares-j14.vercel.app/api/auth/callback"]
require_email_verification = true
verify_email_method = "code"
reset_password_method = "code"
disable_signup = false

[auth.password]
min_length = 10
require_number = true
require_lowercase = true
require_uppercase = true
require_special_char = true

[auth.smtp]
enabled = true
host = "smtp.gmail.com"
port = 465
username = "oreobot94@gmail.com"
# password is managed via secrets — run `insforge secrets add SMTP_PASSWORD "<value>"`
password = "env(SMTP_PASSWORD)"
sender_email = "oreobot94@gmail.com"
sender_name = "J14Oficial"
min_interval_seconds = 70

[storage]
max_file_size_mb = 50

[realtime]
retention_days = 0

[schedules]
retention_days = 7
